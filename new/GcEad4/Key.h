#ifndef __KEY_H
#define __KEY_H

#include <QString>


/*class Key
{
public:
	//static Key empty;

public:
	const QString kind;
	const Key parent;
	const QString name;

public:
	Key(const QString& kind, const QString& name)
		: kind(kind), parent(), name(name)
		{}

	Key(const QString& kind, const Key& parent, const QString& name)
		: kind(kind), parent(parent), name(name)
		{}

	bool isValid() const;

private:
	Key()
		: kind(), parent(), name()
		{}
};*/

#endif
