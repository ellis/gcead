#ifndef __ENTRY_H
#define __ENTRY_H

#include <QVariantMap>

#include "Key.h"


/*class Entry
{
public:
	const Key key;
	const QVariantMap values;

	Entry(const Key& key, const QVariantMap& values)
		: key(key), values(values)
		{}
};*/

#endif
